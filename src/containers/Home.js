import React from 'react';
import PropTypes from 'prop-types';

const Home = props => {
    return (
        <div className="home">
            <h1>Home</h1>
        </div>
    );
};

export default Home;
