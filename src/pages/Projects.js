import React from 'react';
import PropTypes from 'prop-types';

const Projects = props => {
    return (
        <div className="home">
            <h1>Projects</h1>
        </div>
    );
};

export default Projects;
